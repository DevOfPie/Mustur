# Queue

Append a line. Don't name it, don't structure it, don't think about where it
goes. Triage sorts it out later.

This is also where an idea goes when it arrives **while you are working
something else** — append it here and carry on.

Anyone may append, including agents. Attribute yours.

Format: `- YYYY-MM-DD (who) — the thing, as loosely as you like`

**Nothing here is scheduled work.** A line is a note, not a commitment.

Append at the bottom, always.

---

<!-- new lines below -->
- 2026-08-19 (whippy) — queue.md itself will earn findings-queue audit findings (no evidence/review columns); reshaping it into the specified table vs keeping the jot log is a real choice someone should make before milestone 2b
- 2026-08-19 (whippy) — PR #1's body promises three open design questions in docs/ui-surfaces.md; the file marks two (composer, records) — find or retire the third
- 2026-08-19 (whippy) — Anthropic's paused metering change (support article 15036540) would move claude -p / Agent SDK usage off plan limits onto separate credits if it un-pauses; which invocation path the adapter uses decides Mustur's exposure — candidate Plan.md limitation row
- 2026-08-19 (whippy) — records/ now carries the same decisions and findings as decisions.md and queue.md, one addressable and one prose; milestone 2b should decide whether the repository's own StrucGu roles move to the export or stay on the files a person edits
- 2026-08-19 (whippy) — a seeded record's one-line summary can drift from the prose it links to and nothing detects it; a check comparing the two is cheap and nobody has asked for it
- 2026-08-19 (whippy) — `make check` now needs Go on the CI runner and ci/proposed/ci.yml is what pins it; until the owner applies that proposal the runner's preinstalled toolchain is whatever the image happens to carry
- 2026-08-20 (whippy) — workflow.md's "superseded decisions stay, with a pointer" and "never edit an entry" cannot both hold if the pointer goes in the superseded entry; milestone 2 read it as the later entry carrying the pointer, and the contract should say which
- 2026-08-20 (whippy) — nothing checks that the mandate clause in CLAUDE.md still says what milestone 1 scored; a string comparison against the fixture would be a few lines and no milestone has asked for one
- 2026-08-20 (whippy) — no automated check detects records/ drifting from the store, because the committed gate runs `mustur verify` without a store and CI has none to compare against; needs a design, not a line
- 2026-08-20 (whippy) — ci/proposed/README.md says a new check needs the owner: No, and reaches the next push; milestone 2's checks needed a workflow change the token cannot push, so that claim is falsified and the file is where the fix belongs
- 2026-08-20 (whippy) — should Mustur hold a triage rule as a record at all? Four of StrucGu's five roles are record kinds and triage-rule describes a document; nobody has asked for the fifth
- 2026-08-20 (whippy) — the store has no way to add a record after the seed, so every decision since milestone 2 has gone in by editing the seed and re-exporting from a scratch database; the live store cannot take them at all, and the seed refusing a non-empty store is what makes that true
- 2026-08-21 (whippy) — mustur.service passes --export at the live checkout's records/, so a boot-started daemon rewrites tracked files on whatever branch happens to be checked out, and make check's verify-records gate reads a directory that daemon can change mid-run; the export target is also hardcoded, so the unit breaks if the checkout moves. Needs a design (export somewhere the daemon owns? commit on filing? neither?), not a line
- 2026-08-21 (whippy) — commit 0a0a0ac carries three topics (shallow-clone skip, conformance counting, the systemd unit) against workflow.md's one-topic gate; splitting it means rewriting a pushed commit, which needs a force-push nobody here should be doing, so it stands as a defect on the record rather than a fix
- 2026-08-21 (whippy) — MUS-W-0005 and MUS-W-0010 were amended in the store, but internal/seed/data/work-units.json still ships the text they superseded, so a fresh clone seeds records that disagree with records/; the seed is history by design and the amendment log lives only in the store, and which of those a clone should get has never been decided
- 2026-08-21 (whippy) — nothing watches the origin behind Access: the hostname answers 302 whether or not mustur.service is running, so every check in docs/ingress.md passes against a dead box. Restart=always covers a crash, not a unit that failed to start
- 2026-08-21 (whippy) — the seed still ships milestone 3 and 4's pre-split done-when in internal/seed/data/milestones.json, as it ships the superseded MUS-W-0005 and MUS-W-0010 text; a fresh clone therefore seeds milestones that disagree with records/, and the seed-is-history decision has never been reconciled with the store being amended
- 2026-08-21 (whippy) — the decision queue was built from docs/ui-surfaces.md's brief rather than from its artboard in the published visual plan, which is the same route intake took and the thing the plan was published to stop; whether it gets redesigned from the plan or the plan gets amended to match is nobody's call yet
- 2026-08-21 (whippy) — queue.md's line calling the 0a0a0ac commit split impossible without a force-push overstates it: that commit is on an unmerged branch whose own workflow already presumes rebasing, so the option was available and was not taken
- 2026-08-21 (whippy) — nothing on the mustur_route path tells a session that a question is open; the gate catches it at make check, which is the end of the work rather than the start, and the mandated call is the surface that could say it first
- 2026-08-21 (whippy) — the queue-redesign line above is answered: MUS-Q-0010 settled it and the queue was rebuilt from the artboard, so that line is closed rather than open
- 2026-08-21 (whippy) — the `::` option separator only tolerates itself in the third part, so an option whose label or one-line contains it silently loses text; nothing rejects or warns, and MUS-W-0015 records it as a risk rather than a guard
- 2026-08-21 (whippy) — intake has no tab bar, so the only route from it to the queue is a banner that disappears when nothing is open; the queue can be reached from intake exactly when it has nothing to say
- 2026-08-21 (whippy) — the session ownership option is not a security boundary: anyone who can run tmux as this user can set @mustur_started by hand. It separates Mustur's sessions from a person's, not from an adversary's, and nothing in the threat model has ever been written down
- 2026-08-21 (whippy) — `mustur session start --cmd` runs whatever it is given; nothing checks the command is an agent CLI rather than anything else on the machine, and the answer path can then type into it
- 2026-08-21 (whippy) — every claim of the form "verified against real tmux" in this repository was verified with throwaway stores that no reader can reproduce from the tree; 2c cites docs/ingress.md for its method and the session work cites nothing comparable
- 2026-08-21 (whippy) — the intake→queue routing defect from the 4a review is fixed: both surfaces now carry the bar. The line above recording it stands, because the fix came from the owner loading the site and finding only the intake box, not from anyone reading the queue
- 2026-08-22 (whippy) — supervision only runs while a viewer is attached: the reader is started by a viewer, so a session nobody has opened is not being watched and its exit is noticed by nobody. Watching every owned session means polling tmux for sessions nobody is reading, which is a trade nobody has made
- 2026-08-22 (whippy) — the session view appends raw output into a <pre>, so a full-screen agent TUI renders as its ANSI control sequences. Nothing interprets or strips them, and which of those two it should do is a design question
- 2026-08-22 (whippy) — five consecutive reviews have found decisions taken and written into decisions.md rather than raised as prompts. The gate cannot see them, because it reads records/questions.md and a decision never raised leaves no question. Whatever would catch this is not a check over the tree
- 2026-08-22 (whippy) — three invented citations this session, all three bare identifiers in `.go` comments, all three caught by a reviewer rather than a gate: `mustur verify` reads `records/` and never opens a source file. The identifiers in Go comments are the one place this repository asserts a record exists and nothing checks. Whatever fixes it is not a resolution to be more careful
- 2026-08-22 (whippy) — the 4c questions were written into `records/questions.md` by hand, which its own header forbids, and every gate stayed green because every gate reads the export rather than comparing it to the store. `mustur verify` checks the export against itself; nothing fails when the export and the store disagree
- 2026-08-22 (whippy) — six consecutive reviews now, and 4c is the first where the decisions taken in prose included one the owner had already ruled out in principle: pairing a task to a sub-agent by order is an inference, in a package whose header says rows are never inferred. Being able to write the prohibition and cross it in the same file is the thing to fix, and it is not a gate
- 2026-08-22 (whippy) — a nested sub-agent's launching call carries an `agent_id`, so it records as its parent's activity rather than as a launch, and the nested row draws with no task. Untitled rather than wrong, which is the right failure, but the nesting the CLI exposes is not represented at all
- 2026-08-23 (whippy) — a Mustur-started session can sit on a modal it cannot get past on its own: the first `Demo` session landed on Claude Code's MCP-trust prompt for the project, and keystrokes sent to the composer went into that dialog rather than to the agent. Mustur reports the pane as running because it is; nothing distinguishes running from waiting-on-a-dialog, which is the same shape as the `waiting-for-input` inference the owner declined on MUS-Q-0005 and wants a decision rather than a guess
- 2026-08-24 (whippy) — two of the owner's own decisions now disagree and neither is wrong: MUS-Q-0018 put an always-writable composer on the session view, and MUS-D-0013 asks for one capture surface "instead of keeping two capture muscles". Both are built, so a message can be started in three places — the session view's reply box, the composer, and the intake box. workflow.md says a conflict between decisions is a bug to report rather than one to pick, so it is reported here and not resolved
- 2026-08-24 (whippy) — `docs/ui-surfaces.md` has now had three surfaces built the way it exists to prevent, two of them after such a record existed, and the owner has answered the same question twice (MUS-Q-0010, MUS-Q-0034). The record is demonstrably not the safeguard. Whatever would be one is not another paragraph in that file
- 2026-08-25 (whippy) — the line above is now seven surfaces, not three: milestone 5b built four authentication surfaces at once with no plan at all, drew them afterwards under MUS-Q-0043, and rebuilt from the drawings. Four instances in one milestone, after two prior records and two owner answers on the same subject, is the same line failing rather than a new one. Recorded as MUS-F-0027 so the pattern has an identifier the way a defect does
- 2026-08-25 (whippy) — no surface knows the role of whoever is reading it, so a reader is shown a `/sessions` tab that 403s and an intake form whose POST is refused. The guard refuses correctly; the pages have not caught up. Deferred rather than fixed because the plumbing is the same for both and the tab bar's final shape is MUS-Q-0052's, so fixing it now would be rework
- 2026-08-24 (whippy) — nothing enforces that a `###` entry in decisions.md gets an index row, and 18 were missing before this milestone fixed them — every one added during this session. `check-links` cannot see it because a missing row breaks no anchor, and the index states the rule it does not keep
- 2026-08-26 (whippy) — `mustur serve --sessions` cannot be stopped while a pane is being piped: the pipe reader blocks in uninterruptible sleep, systemd's stop times out at 90s, and the unreaped process holds the listening socket so the site answers nothing in the meantime. Measured during an ordinary binary swap, which took mustur.devofpie.com down for about ten minutes. `tmux pipe-pane -t <pane>` releases it. MUS-F-0030; the real fix is a shutdown path that closes the readers rather than leaving them to the kernel
- 2026-08-26 (whippy) — the session view's seed and its live stream are captured differently and read differently: `capture-pane` without `-e` strips escapes, `pipe-pane` sends the pane raw, and the client appends both to `textContent`. So colour codes and OSC 8 hyperlinks land on screen as literal text the moment the first live byte arrives. Under that sits a bigger mismatch — the pane runs a full-screen TUI that repaints by moving the cursor, and appending its bytes linearly stacks partial frames. MUS-F-0031. Stripping escapes is the cheap half; whether to strip, interpret, or render frames is a design decision
- 2026-08-26 (whippy) — the intake box takes text only, and the owner tried to send a screenshot through it. A picture is the fastest way to report a layout defect and there is no route for one: the builder suggested intake without checking. Worth deciding whether intake accepts an image, or whether some other path onto the machine is the answer
- 2026-08-26 (whippy) — searching the destination list, noted by the owner when the chips became a dropdown. Not built: with six destinations a search box is furniture, and a native select already takes type-ahead on a desktop. Worth revisiting when a second project is onboarded and the list stops fitting on a screen
- 2026-08-26 (whippy) — the placeholder destinations are gone; the flag, the code and its test are removed rather than left dormant. What the padding answered: a native select does not grow with its list, rendering in the same 358px on a phone and 640px on a laptop at sixteen options as at six, in both browsers. Only the opened popup grows, which is what a search box would be for
