# LNK-W-0028

<!-- Generated by `mustur export`. Do not hand-edit: `mustur verify` fails on drift, and the store is the record. -->

**Organization deletion and tenancy teardown**

work-unit · 2026-08-01

milestone: [LNK-M-0028](../milestones.md#lnk-m-0028)

cites: [LNK-M-0027](../milestones.md#lnk-m-0027)

cites: [LNK-D-0018](../decisions.md#lnk-d-0018)

cites: [LNK-D-0034](../decisions.md#lnk-d-0034)

cites: [LNK-D-0037](../decisions.md#lnk-d-0037)

cites: [LNK-D-0032](../decisions.md#lnk-d-0032)

cites: [LNK-D-0036](../decisions.md#lnk-d-0036)

cites: [LNK-D-0031](../decisions.md#lnk-d-0031)

cites: [LNK-D-0016](../decisions.md#lnk-d-0016)

cites: [LNK-M-0016](../milestones.md#lnk-m-0016)

cites: [LNK-D-0005](../decisions.md#lnk-d-0005)

cites: [LNK-F-0028](../findings.md#lnk-f-0028)

cites: [LNK-M-0036](../milestones.md#lnk-m-0036)

## Context

**Depends on:** LNK-M-0027. The edge is hard: deleting an organization means
deleting the memberships, workspaces and invitations LNK-M-0027 is what first lets a
person create.
**Discharges:** nothing on the scope tables. Owner-added scope, 2026-08-01,
placed when LNK-M-0027's audit bullet was found to require an operation the
product does not have. It also gives `org.delete` — seeded in Phase 1's
`00700_seed.sql` and granted to `owner` alone — the first operation it has ever
gated.

An organization is the one tenancy object with no way out. Every other object
this product creates can be removed by the person who made it; an organization
accumulates workspaces, links, members, invitations and audit history and then
stays forever. This milestone is the exit, and almost all of it is about what
deletion must *refuse* rather than what it removes.

#### Done means

##### The operation

- `DELETE /api/v1/organizations/{id}` and a UI path, gated by `org.delete` —
  its first enforcement. `org.delete` is held by the `owner` role only, and
  LNK-M-0027's rank rules already forbid an admin from acquiring it.
- Its API-key delegability is decided and recorded against
  D18,
  naming which limb it matched or that it matched neither, per the inherited
  permission rule.
- Deletion is one transaction. A partially deleted organization is the one
  outcome worse than an undeletable one, because it leaves members resolving
  into workspaces that no longer exist — the state
  D34
  exists to prevent.

##### What it refuses

Each refusal is asserted by its own test, and each names what an operator does
instead.

- **An organization still holding links** — refused, archived links included
  (**D37**, answered 2026-08-01, mirroring
  D32).
  An org-level cascade through the same links would make D32 bypassable by
  deleting one level up. The workspaces must be emptied first, and with no bulk
  delete until 2+ that is a link at a time — a cost this milestone states rather
  than hides.
- **The instance's last organization**, on the same reasoning that refuses the
  last owner and the last workspace: an instance with no organization has no
  path back that does not involve SQL.
- **It does *not* refuse on account of orphaned members.** See D36 below — that
  case was answered the other way, and it is what makes this milestone large.

##### Belonging to nothing, which D36 makes a real state

Answered 2026-08-01, before code, as this file required. Deletion proceeds even
when a member has no other organization. Consequently:

- The account **survives** with no membership. Nothing deletes an account as a
  side effect of deleting an organization.
- `ResolveWorkspaceForUser` currently treats *no workspace* as a broken
  instance. It must treat it as an **empty state**, and every signed-in surface
  must render that state rather than erroring — asserted by test, not by
  clicking around. This is the session path
  D31
  deliberately left untouched during LNK-M-0027, so it is handled here with
  its own tests rather than as a side effect.
- On next sign-in the account is **prompted to create an organization**, and can
  take no other action until it has one — asserted by a test that signs in as an
  orphaned account and finds every other surface refused.
- **First-organization creation must be reachable by an account holding no
  permissions.** An account with no membership holds no role, so it holds no
  `orgs.create`, which
  D16
  grants through the `owner` role and which `POST /organizations` enforces
  today. The milestone **records which mechanism it used** and why that is not a
  second authorization axis — D16's rule is that the permission is a grant
  rather than a check on *how an account was made*, and a membership count is a
  check on present state. Getting this wrong is how RBAC quietly grows a
  parallel path, so it is named here rather than left to the build.

##### What it leaves behind

- **Audit rows survive the organization they describe.** `organization.deleted`
  is emitted (LNK-M-0016), and the trail for a deleted organization stays
  readable — a tenancy teardown that erased its own record is the one shape an
  audit log must not have. The retention rule stays D5's.
- **The aliases of trashed links that had received traffic survive**, in
  `reserved_aliases`, written in the transaction that deletes them. Nothing else
  does. The reason the *link guard* does not already settle this is the whole of
  F28 and is written out under the reopening below: the guard counts live links
  and excludes soft-deleted ones on purpose, so a teardown reaches the cascade
  still holding trashed links for the rest of their trash window, and the purge
  job — the only other writer of `reserved_aliases` — never sees them. The
  threshold is `PurgeExpiredLinks`', so an alias that never received a click is
  released rather than reserved.

##### Enforcement

- The refusals are asserted by tests that fail when the guard is removed, not by
  review. Each guard locks the rows it counts before counting, the way LNK-M-0027's
  last-owner refusal does, so two administrators acting at once cannot each pass
  a check the other invalidates.
- `organization.deleted` joins `internal/audit`'s action vocabulary, and the
  comment at `audit.go` explaining why it was absent is removed rather than left
  to contradict the code beneath it.

#### Reopened 2026-08-01 — the aliases are not reserved, and the file says they are

LNK-M-0028 shipped and its *What it leaves behind* claim turned out false. Reopened
by the owner on LNK-M-0036's triage.

**What is false.** The bullet above **used to read** *"Whether anything else
survives — the aliases, so they cannot be re-registered by somebody else, or
nothing at all — is stated explicitly, with its reason"*, and the reason given
in `organization.go` was that *"an organization that reaches this line holds no
links, so every alias it ever had was released by the link deletions that had to
happen first, and the ones that had received traffic are already in
`reserved_aliases` where the purge job put them."* Both are quoted here as they
stood; both have since been rewritten to say what actually survives. The
organization **does** hold links at that line — soft-deleted ones, for up to
`TrashRetentionDays` (30 days) — because both emptiness guards exclude them on
purpose. The cascade then hard-deletes those rows without ever running
`ReserveAlias`, and a trafficked alias returns to the namespace. Recorded as
F28; the fix is in spec here.

Reproduced on a live database inside `BEGIN … ROLLBACK`: a trashed link with
`click_count = 4242`, both guards counting **0**, `DELETE FROM organizations`,
**zero** `reserved_aliases` rows written, and `IsAliasTaken` flipping `t` → `f`.

##### Done means, for the reopening

- **`DeleteWorkspace` is covered as well as `DeleteOrganization`.** This is the
  trap: the workspace half predates LNK-M-0028, so a fix aimed only at the milestone
  that carries the false paragraph leaves the shorter path open. Any workspace
  writer can reach it.
- **Trafficked trashed aliases are reserved in the same transaction as the
  delete**, or the delete refuses while they exist. Either is acceptable; the
  reasoning for the choice goes in decisions.md, because refusing changes a
  shipped operation's behaviour and reserving changes what teardown leaves.
- **The threshold matches `PurgeExpiredLinks`.** It reserves `WHERE
  click_count > 0`, and `reserved_aliases`' own rationale is that aliases which
  never received a click are released so the namespace does not bleed.
- **The `deleted_at`-excluding guards stay as they are.** `members.sql:177` and
  `organizations.sql:60` exclude soft-deleted links deliberately — *"counting it
  would leave the workspace undeletable until the purge job ran"* — and that
  reasoning is still right. The defect is what the delete does next, not what
  the guard counts.
- **A test that deletes a workspace holding a trashed, trafficked link and then
  asserts the alias is still taken.** `IsAliasTaken` deliberately has no
  `deleted_at` filter, so the assertion is meaningful before the delete too.
- **The paragraph is rewritten to say what actually survives.** It is the claim
  that was false, and leaving it while fixing the code would make the file
  right for the wrong reason.

#### Deliberately not in this milestone

- **No soft delete, no restore, no trash — for organizations.** Links have all
  but one of those already: Plan.md ships *soft delete (30-day recovery)* in
  Phase 1, and that window is `TrashRetentionDays`, which is the very thing the
  reopening above turns on. What links lack is the way *back* — nothing in the
  product clears `deleted_at`, so the trash is a waiting room rather than a
  restore path. An organization gets none of the three, this is not the
  milestone that introduces them, and deletion here is irreversible and says so
  in the confirmation surface.
- **No export before deletion.** Taking your data out is its own feature and
  belongs with bulk operations, which Plan.md already places in 2+.
- **No scheduled or delayed deletion.** A grace period is a state machine and a
  job, and both want a milestone that is about them.

#### Risks

The central question is now answered (D36) and the answer is the expensive one,
which moves the risk rather than removing it. Most of this milestone is no
longer deletion at all — it is teaching the session path that *belongs to
nothing* is a state a signed-in person can legitimately be in. That path is
reached by every authenticated request, so a mistake there is an availability
bug for everyone rather than a bug in a teardown feature almost nobody runs.
The mitigation is that the new state is asserted by test at the session layer
and not merely rendered correctly on the page that prompts for an organization.

The second risk is the `orgs.create` seam. An account with no permissions being
offered a way to create something is exactly the shape that grows a second
authorization path beside RBAC, which is what D16 was written to prevent. The
milestone names its mechanism for a reason.

The second risk is that this milestone is the first to delete rows Phase 1's
schema only ever inserted, so the cascade behaviour in `00200_identity.sql` and
`00300_links.sql` is being exercised for the first time against real fixtures.
What the foreign keys actually do on delete is a tree fact to establish, early,
rather than an assumption to build on.

---

Rules every milestone inherits — SLO re-verification, privacy stance, permission
seeding, sabotage discipline, OpenAPI and contract parity — are in
README.md and are **not** repeated per file.

## LinkCtrl

phase-details/m28.5.md
