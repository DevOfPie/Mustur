# LNK-W-0064

<!-- Generated by `mustur export`. Do not hand-edit: `mustur verify` fails on drift, and the store is the record. -->

**The QR tab's third report, and the first script the dashboard depends on**

work-unit · 2026-08-14

milestone: [LNK-M-0064](../milestones.md#lnk-m-0064)

cites: [LNK-M-0063](../milestones.md#lnk-m-0063)

cites: [LNK-M-0060](../milestones.md#lnk-m-0060)

cites: [LNK-M-0062](../milestones.md#lnk-m-0062)

cites: [LNK-F-0238](../findings.md#lnk-f-0238)

cites: [LNK-M-0065](../milestones.md#lnk-m-0065)

cites: [LNK-D-0193](../decisions.md#lnk-d-0193)

cites: [LNK-D-0183](../decisions.md#lnk-d-0183)

cites: [LNK-D-0189](../decisions.md#lnk-d-0189)

cites: [LNK-F-0237](../findings.md#lnk-f-0237)

cites: [LNK-M-0061](../milestones.md#lnk-m-0061)

cites: [LNK-F-0244](../findings.md#lnk-f-0244)

cites: [LNK-D-0178](../decisions.md#lnk-d-0178)

cites: [LNK-F-0239](../findings.md#lnk-f-0239)

cites: [LNK-F-0246](../findings.md#lnk-f-0246)

cites: [LNK-D-0196](../decisions.md#lnk-d-0196)

cites: [LNK-F-0242](../findings.md#lnk-f-0242)

cites: [LNK-F-0240](../findings.md#lnk-f-0240)

cites: [LNK-F-0247](../findings.md#lnk-f-0247)

cites: [LNK-M-0074](../milestones.md#lnk-m-0074)

cites: [LNK-D-0203](../decisions.md#lnk-d-0203)

cites: [LNK-F-0249](../findings.md#lnk-f-0249)

cites: [LNK-D-0206](../decisions.md#lnk-d-0206)

cites: [LNK-D-0204](../decisions.md#lnk-d-0204)

cites: [LNK-F-0250](../findings.md#lnk-f-0250)

## Context

**Depends on:** LNK-M-0063 *(hard — eight of the ten limbs edit controls
LNK-M-0063 built, and building against the pre-LNK-M-0063 row would lay out a surface that
no longer exists)*, LNK-M-0060 *(hard, for the list's sort and the last code's
remove button)*, LNK-M-0062 *(hard — the logo section this milestone
folds away is the one its second reopening rewrote)*.
**Discharges:** F238, owner-approved as work
2026-08-14. Owner-added scope the same day, from the third QR-tab report.

The tab has now been reported three times. The first two produced reopenings and
LNK-M-0063; this is the third, and what makes it a milestone rather than
four reopenings is the same argument LNK-M-0063's file makes — most of these items
are not defects. Three of them **reverse a choice this repository argued for in
writing**, which is stated per bullet rather than left for a reader to notice.

**This milestone exhausts the LNK-M-0060 band.** `M50.9` is reserved for reviews
(planning.md §4), so nothing can be inserted
between LNK-M-0060 and LNK-M-0065 after this. An eleventh item on this tab is a reopening or a
Phase 4 row, and that is now a fact about the numbering rather than a preference.

#### Done means

##### The list stops moving under the reader

- **Codes are listed alphabetically by name, case-insensitively**, and the
  order does not change when the default moves. Owner-set:
  *"Selecting a different default code re-orders the list of codes which can
  make it seem like the selection didn't change."*
- **This reverses `ListQRCodes`' stated design.** `internal/store/query/campaigns.sql:167`
  is `ORDER BY (NOT (q.is_default OR q.slug = '')), q.created_at, q.id`, and the
  comment above it defends the re-order in as many words — *"the list re-orders
  when the reader moves it, which is the visible half of what setting a default
  does"*. That argument is not withdrawn; it is answered. The filled icon
  LNK-M-0063 put on every row is what makes the change visible now, so the sort no
  longer has to carry that job, and carrying it was what made the change look
  like nothing happened.
  *Enforced by:* an **integration** test asserting the returned order for a set
  whose alphabetical and creation orders differ, **and** that moving the default
  does not change it. The sort is `lower(q.label)` with a stable tie-break, so
  it matches `ORDER BY lower(c.name), c.id` at `:49` rather than inventing a
  second collation for the same product.
  *(Amended at step 3.4: this read "an `internal/link` test". The order is SQL —
  a unit test there would have to fake `dbgen.Querier` whole and would assert
  the fake. The test drives `link.Service.ListQRCodes`, the surface this bullet
  names, against Postgres. D193 argues it.)*
- **The default is still identifiable without reading**, which is what the
  sort used to buy: the filled icon (`partials/link_qr.html:257`) and the meta
  line's *"the default"* both stay.
- **Three records cite the old order and are part of this milestone, not
  cleanup.** `internal/store/query/campaigns.sql:169-176` justifies promoting
  the oldest code by *"the promoted code is the one at the top of the list the
  reader is looking at"* — which alphabetical breaks —
  `internal/link/qr.go:247` says the service *"returns every code a link
  carries, default first"*, and `internal/httpx/web_links.go:675` says the same
  of the view field it fills from that service: *"The link's codes (LNK-M-0060),
  default first"*. **The promotion rule itself does not change**: D183
  set it to the oldest and nothing here reverses that; what changes is the
  sentence explaining why it looks natural, which stops being true. All three
  are corrected in this milestone's diff. *(The first two were found by the plan
  review as a hidden edge; the third by the milestone's reviewer, and the count
  was amended at step 3.4 — see decisions.md.)*

##### The size control tells the truth while it moves

- **The slider and the number follow each other, live, in both directions.**
  Owner-set: *"Dragging the Size slider doesn't change the number next to it
  until you save. The number should change with the slider as well as the
  reverse."*
- **This is the first script this product's dashboard depends on**, owner-decided
  2026-08-14 over dropping one of the two inputs. *(Not the first script it
  writes: `internal/ui/static/js/docs.js` is 34 hand-written lines booting
  Swagger UI on `/docs`, written because the CSP forbids inline script. The plan
  review caught the overclaim. The edge that is real, and the one the precedent
  argument rests on, is that no script has ever driven a **control on the
  dashboard** — docs.js boots a vendored viewer on a page of its own.)*
- **It reverses a written argument**, which this milestone names wherever it
  does one: `partials/link_qr.html:485-489` says *"Nothing in this browser can
  copy a dragged slider into the box beside it: this dashboard runs htmx and no
  script of its own, and a `script-src 'self'` page is not the place to
  introduce one for a form control."* That is the sentence the owner overruled.
  What it defends — the arbitration between `size_shown` and the requested
  size — **must survive as the script-blocked path**, which is the degradation
  bullet below.
- **What it does not cost.** A served file under `script-src 'self'`: the CSP is
  **unchanged**, no `unsafe-` waiver, no Node, no CDN, no build step, which is
  the whole of what the inherited *`ui` stays stdlib-only* rule asks.
  `htmx.min.js` and `docs.js` are already served from the same directory.
  *Enforced by:* a case in the kept browser suite dragging the slider and
  reading the number, and typing in the number and reading the slider. **A unit
  test cannot see this** — it is the one claim in this milestone that only a
  browser can make, and it is why the suite is the enforcement rather than a
  template assertion.
- **It degrades to today's behaviour with the script blocked.** The number
  input keeps its own value and the form still submits it, so a reader with
  JavaScript off loses the live echo and nothing else.
  *Enforced by:* a template test asserting both inputs still carry `name=` and
  their values, and a browser case with scripts disabled asserting the form
  still saves.

##### A save returns you to where you were

- **A style write lands back at the QR tab's scroll position**, not at the top
  of the page. Owner-set: *"The whole page resets to the top every time the
  save button is used… a force to top is jarring and bad UX."*
- Every write redirects through `qrReturn` (`internal/httpx/web_qr.go:103`) and
  none of them carries a fragment, so the browser lands at the document top.
  **The milestone does not specify the mechanism** — a fragment on the redirect
  and a scroll restoration in the script are both available, and which is right
  depends on whether the tab's position or the control's is what a reader
  expects to return to. The build chooses, argues it in a decision entry, and
  says what it rejected.
  *Enforced by:* a browser case scrolling down the tab, saving, and asserting
  the reader is still where they were.
  *(Amended at step 3.4: this read "scrolling to the size control … asserting
  the control is still in the viewport". The case scrolls to and asserts the
  **Save** button, which is the control a reader is actually on when a write
  happens and is what makes the assertion distinguish a restored position from
  the `#qr` fragment's own landing. The claim — a save returns you to where you
  were — is unchanged and is what the case tests.)*

##### The tab says less, again

- **Two sentences go**, both owner-set and both quoted here so the diff is
  checkable against the report rather than against a summary:
  - `partials/link_qr.html:346` — *"What a second code buys is knowing which one people
    scanned."* Owner: *"this is something that would only be appropriate on a
    help page."*
  - `partials/link_qr.html:316` — *"A link always has a QR code, so this one cannot be
    removed. Add a second and either becomes removable."*
- **The prose bound moves down with them.** LNK-M-0063's bound is 900
  characters by a stated method and the tab measures 877. Measured by that same
  method against the tree at `ea656bd`, `:316` is **102** characters and `:346`
  is **60**, and the logo section's prose moves rather than leaves. *(The plan
  first said 171 and 60. 171 is the **pre-LNK-M-0063** last-code paragraph from the
  `d7107b1` baseline — a stale number attached to the wrong sentence, caught by
  the plan review.)* **The build re-measures by LNK-M-0063's method and sets the new bound to
  the measured figure rounded up to the next fifty**, so the number stays one a
  later milestone has to work at rather than one it clears by accident.
  *Enforced by:* `TestTheQRTabsProseIsUnderItsBound`, with its number updated
  and its reasoning beside it.

##### The last code's remove button is disabled rather than absent

- **The button renders on every row**, and is `disabled` on a link's only code
  with a tooltip reading *"Every link must have at least 1 QR code."*
  Owner-set, and the sentence is the owner's wording.
- **This reverses what `partials/link_qr.html:266-269` argues** — *"Absent rather than
  disabled: the sentence below the list is where the reason goes, because a
  disabled control with no explanation reads as a bug."* The reversal is
  narrow and it is the good kind: that comment's objection is to a disabled
  control **with no explanation**, and this gives it one. The sentence it
  points at is the one being deleted above, so after this milestone the
  explanation lives on the control instead of below the list.
  *Enforced by:* a template test rendering a one-code link and a two-code link,
  asserting the button is present in both and `disabled` with its reason in the
  first. `Restore defaults` ships the nearest thing to this shape
  (`partials/link_qr.html:716-717`) — but its reason is a visible `<p>` beside it
  at `:719`, not a tooltip, so this milestone follows its *disabled with a
  reason* principle rather than copying its markup. The plan review caught the
  overclaim.
  *(Line numbers amended at step 3.4, twice over: they read `:545` and
  `:546-548`, written against `d7107b1` and stale before this milestone began.
  Its own diff then moved them a further seven lines. This is the citation
  hazard the last run recorded from the other direction — a citation written
  against a diff in flight goes stale before the diff lands, so it is re-read
  after the diff rather than during it.)*

##### Adding a code is a prompt, not a form in the page

- **A `+` icon button beside the `N/20` counter** opens a prompt carrying a
  text field and an add button. It replaces the *Add another code* label, text
  field and *Add the code* submit at `partials/link_qr.html:331-337`.
- **The mechanism is the popover LNK-M-0063 already built**, not a new one:
  `partials/link_qr.html`'s row menus and `partials/nav.html`'s header menus are
  both `popovertarget`/`popover="auto"`, and this is a third instance of the
  same idiom rather than a dialog. It anchors to the counter, which is a single
  element, so it needs no `anchor-scope` — that machinery exists because twenty
  rows needed twenty anchors (D189) and one counter needs
  one.
  *Enforced by:* a template test asserting one add control, that the old label
  and submit are gone, and that the field posts what the handler reads; plus a
  browser case opening it and adding a code.
- **The recorded popover exception widens, and the milestone says so rather
  than letting the assertions notice.** `internal/ui/link_qr_test.go`'s
  `TestTheQRSettingsRenderInTheTabsFlow` and `internal/ui/panel_test.go`'s
  `strayPopoversIn`/`qrRowMenusIn` all refuse a popover inside `<main>` whose id
  is not prefixed `qr-download-`, which is D189's exception
  as written. This add prompt is a third popover on the surface, so those three
  assertions are amended **in this milestone's diff** to name it — the same
  silent-extension shape D189 was itself written to prevent. Widening them by
  discovering a red test is not the same as widening them on purpose.
  F237 warns the matcher is fragile to any new
  popover spelling, which is a reason to check the new one is seen rather than
  a reason to close that row here.
- **The `+` icon is new** and goes in `partials/icons.html` on that file's
  shape, which is m47.5.md:79-81's convention and LNK-M-0063's
  precedent for four more.

##### The controls answer the pointer

- **Hover feedback is visible on the selected row.** `partials/link_qr.html:162` paints
  the selected `<li>` `bg-sunken` and the row's controls all hover to
  `hover:bg-sunken` (`:211`, `:261`, `:278`), so on the row a reader is most
  likely to be on, the two controls they use most give no feedback at all.
  Owner-reported. **This one is a defect rather than a preference** — the
  affordance is written and does not appear.
  *Enforced by:* a template test asserting the row's controls do not resolve to
  the selected row's own background, and a browser case hovering a control on
  the selected row and comparing the computed background against the row's.
- **The default control's tooltip reads `Default QR Code` and
  `Make Default QR Code`.** Owner-set, replacing
  *"%s is the default code"* / *"Make %s the default code"*
  (`partials/link_qr.html:257`, `:262`).
  **There are two such pairs and this bullet named one**, amended at step 3.4
  from an enumeration of two sites to one of four: the `links.read` branch
  renders the same control unwritable at `:301` and `:306`, reading
  *"%s is the default code"* / *"%s is not the default code"*. It moves with the
  editor's pair — leaving it behind would put two naming conventions on one
  list, and the glyph goes decorative on both branches alike, so the read-only
  buttons would otherwise have to name themselves in the wording being replaced.
  Its second string has no owner wording to copy, `Not the Default QR Code`
  being the build's, since the owner's report names only a control they can
  press. See decisions.md.
  **The name loses the code's name and that is the owner's call**, taken
  knowing a screen-reader user hears the same string on twenty rows; the row's
  own link carries the name immediately before it.
  **This reverses assertions LNK-M-0063 wrote**, which is the fourth written argument
  this milestone overturns: `internal/ui/link_qr_test.go` asserts the current
  strings at `:798-799`, `:882-883` and `:889`. They are amended in this
  milestone's diff, named here rather than discovered as three red tests.
- **The tooltip's hover zone is the whole button.** Today the tooltip is a
  `<title>` inside the icon, which `partials/icons.html` draws at `h-3 w-3`
  inside a `p-1.5` button — a ~12px target inside a ~30px control, which is why
  it is hard to show. Owner-set: *"anywhere a click on the button would be
  accepted should also show the tooltip."*
  *Enforced by:* a template test asserting the tooltip is on the button rather
  than only inside the icon, and a browser case hovering the button's edge —
  inside the control, outside the glyph — and asserting the tooltip appears.

##### The logo folds into the style section

- **The `Logo` heading and its section go**, and the upload moves into the
  style **section**. Owner-set: *"Upload a logo should be moved into the style
  section and the header/text/section it is currently in should be fully
  removed."*
- **Section, not form, and the distinction is load-bearing.** `partials/link_qr.html:582-587`
  argues the upload must be its own `<form>`: *"A file needs
  `enctype="multipart/form-data"`, which the style form above cannot carry
  without every one of its other buttons arriving in a body the handler does not
  read."* HTML forbids nested forms, so the input cannot sit *inside* the style
  form without `form=` attribute machinery nothing here asks for. It moves under
  the style heading, in the same visual section, still posting to its own route.
  *(Corrected at the plan review, which caught this bullet asserting a structure
  the tree argues is impossible — it read "the file input is inside the style
  form". The owner's word was section.)*
- The heading is `partials/link_qr.html:635` and the input `:678`.
  **This is LNK-M-0061's and LNK-M-0062's surface**, which
  LNK-M-0063 deliberately did not restructure — *"touching it here would
  put two milestones' work in one commit"*. That reasoning does not bind this
  milestone, which is scheduled to touch it; what carries over is that the
  logo's **behaviour** is not this milestone's, only its placement.
  *Enforced by:* a template test asserting no `Logo` heading renders, that the
  file input renders within the style section's markup, that it still carries
  `enctype="multipart/form-data"` on its own form, and that the logo's own
  controls — upload, replace, remove — all still render and still post where
  they did.

##### Deliberately not done

- **No contrast warning icon.** F238(d) asks for a warning icon with a tooltip
  when the two colours are too close, replacing the sentence that advises it.
  It is **not in this milestone**: the product has no contrast metric — `grep
  -rn -i contrast` over `internal/qr`, `internal/link` and `internal/httpx`
  returns nothing, and its one hit anywhere in non-test Go is a comment at
  `cmd/lctl/demo_phase2.go:1329` — so it is a computation to
  choose and defend, not a control to draw, and choosing between a WCAG ratio
  and a QR-specific luminance delta is a decision the owner has not been asked.
  The advisory sentence at `partials/link_qr.html:573` **stays** until it is. Filed as
  its own row rather than half-built here.
- **No change to what a logo does**, only to where its control sits.
- **No demo seeder row.** LNK-M-0060's and LNK-M-0061's rows already
  seed a link with several codes and a logo, which is what this re-lays-out.
- **No second script.** The file this milestone adds does one thing — bind two
  inputs, and restore a scroll position if the build chooses that mechanism.
  Anything else wanting script is a decision, not a follow-on.

#### Risks

**The script is a precedent before it is a feature.** Ten lines binding two
inputs is easy; what is not easy is that every later control can now cite it.
The milestone's answer is the *deliberately not done* list above and a decision
entry that says what the file is allowed to contain, so the next request argues
against a written bound rather than against nobody.

**A browser-only claim is a claim the unit suite cannot defend.** The live
slider, the scroll restoration and the hover feedback are all only checkable in
a browser, so this milestone leans on `make verify-ui` more than any before it.
A spec that silently stops running looks exactly like a spec that passes —
F237 is the same shape one level down — so each
new case asserts its own precondition rather than only its conclusion.

**Reversing two written arguments invites reversing them back.** The sort and
the disabled-remove-button both had reasons, and both reasons survive their
reversal — the sort's job moved to the icon, the comment's objection is
answered by the tooltip. Recording *why the old argument no longer applies* is
what stops the third report on this tab from being undone by the fourth.

**The prose bound is being moved by the milestone that has to meet it.** LNK-M-0063
set 900 and this sets a lower one from its own measurement, which is the shape
of a gate marking its own homework. The rounding rule above is the guard: the
number comes from a measurement rounded up to the next fifty, not from whatever
the build happened to land on.

---

#### Reopened 2026-08-14 — the fourth report, and a claim this milestone got half right

Owner-scheduled from F244, the tab's **fourth**
report, filed hours after this milestone shipped. Five of its seven limbs are
here; **(b)** and **(e)** are Phase 4 rows and are not.

**Why a reopening rather than a successor.** The LNK-M-0060 band is exhausted —
`M50.9` is reserved for reviews, which this file says above — so no LNK-M-0060.x
milestone can be inserted. More to the point, limb (a) makes *this milestone's*
claim false, and workflow.md reopens rather than succeeds for
exactly that: a successor would leave a `done` row asserting something untrue.
The number does not change, and work added here marks LNK-M-0064.

##### The save returns you to where you were, without showing you somewhere else first

- **F244(a), and it is a defect in this milestone's own new code.** Owner:
  *"immediately upon changing the default the screen reloads at the top of the
  page then jumps down to match the previous scroll position."* The reader does
  arrive where they were. They are shown the top of the card on the way, and
  *"a save returns you to where you were"* is not what a jump-and-settle looks
  like.
- **The mechanism is written into the code that causes it.**
  `internal/ui/static/js/qr-size.js:179-199` says the restore is applied a
  second time *because* the browser performs its `#qr` fragment scroll after
  `DOMContentLoaded` — so the correcting scroll is necessarily after a paint.
  The fragment is put there by `internal/httpx/web_qr.go:131`,
  `return page + "?" + q.Encode() + "#qr"`, and it has been on that redirect
  since D178, long before a script existed to fight it.
- **D193 chose the remembered position over the fragment and left the fragment
  in place.** That is the thing to correct: two mechanisms are aiming at the
  same scroll and the second one exists to undo the first. **The milestone does
  not specify which goes** — removing `#qr` from the link-page redirect, or
  restoring before first paint, are both available and they cost different
  things. `#qr` is also what `restore()` tests for to decide the tab rendered at
  all, and D178 records the fragment as part of the return path, so removing it
  is not a one-line deletion. The build chooses, argues it in a decision entry,
  and says what it rejected.
  *Enforced by:* a browser case asserting the position is **never** somewhere
  else between the load and the settle — not merely that it settles correctly,
  which is the assertion that already passes and did not catch this.
- **The harness could not see this and that is part of the finding.** Two probes
  missed it: one searched for `button[name="default"]` where the control is
  `name="make_default"` and skipped in silence, and one let the driver
  auto-scroll the control into view so the stored position was never the
  reader's. A case that cannot fail is F237's
  shape, and this milestone's own risk section named it. **Every case added
  here asserts its own precondition**, including that the control it drives
  exists.

##### The tab says less, a third time

- **Three sentences go**, all owner-set and quoted so the diff is checkable
  against the report:
  - `partials/link_qr.html:43` — *"Scans count as clicks and appear under
    **qr** in the Analytics tab's referrers breakdown, one row per code."*
    Owner: F244(c).
  - `partials/link_qr.html:233` — the meta line's
    `{{if .Default}} &middot; the default &mdash; a scan carrying no code of its
    own is counted against this one{{end}}`. Owner: F244(d), *"remove the 'the
    default - …' text and its description on the Default item"*.
  - `partials/link_qr.html:850` — the logo's limits sentence. Owner: F244(f),
    *"the user will get an error if it is too big and should not be bothered
    before hitting the issue."*
- **(d) takes the last written statement of what the default *does*.** The
  filled icon and the `Default QR Code` tooltip say *which* code it is; nothing
  left on the tab says an untagged scan is counted against it. That is
  deliberate on the owner's instruction and is recorded here rather than
  discovered later as a gap — `api/openapi.yaml` and `docs/usage.md` still
  carry it, and both stay.
- **The prose bound moves down with them and by the same rule.** Re-measure by
  LNK-M-0063's method and set the bound to the measured figure rounded up
  to the next fifty. It is 750 now, measured 715.
  *Enforced by:* `TestTheQRTabsProseIsUnderItsBound`, its number updated and its
  reasoning beside it.

##### The logo's controls are together

- **The logo section moves above the style form's Save button.** Owner, F244(g):
  *"The logo picker should be above the save button so all changes to the logo
  are together."* **As the reopening found it**, `Save` was
  `partials/link_qr.html:687`, `Restore defaults` `:716` and the file input
  `:861`, so the two controls that write a logo sat below a button that does not
  save them.
  *(Citations amended twice at step 3.4 and the first amendment was wrong. It
  replaced these with the post-fix numbers — `:866`, `:895`, `:725` — which
  describe the tree the bullet asks for while the sentence describes the tree it
  found, so the bullet asserted its own opposite: 725 is above 866. A bullet
  saying **today** is dated to when it was written, and re-pointing it at the
  fixed tree is not a fact correction but a change of meaning. The pre-fix
  numbers are restored and tensed; the fixed positions are the ones above.)*
- **This is placement, and the constraint LNK-M-0064 already recorded still binds:**
  the upload is its own `<form>` because a file needs
  `enctype="multipart/form-data"` and HTML forbids nested forms. It moves within
  the section; it does not move *into* the style form.
  *Enforced by:* a template test asserting the logo controls render before the
  style form's submit in document order, that the upload still posts to its own
  route with its own encoding, and that every logo control still renders.

##### Deliberately not done in this reopening

- **F244(b), the preview vanishing when a code is selected**, is a Phase 4 row.
  It is not a rendering fault: `link_qr.html:236` links each row to
  `{{.Panel}}`, which `internal/httpx/web_links.go:1289-1291` builds as
  `/links/<id>/qr?code=<slug>` — the panel route, a page with no link heading
  row. Answering it means deciding what selecting a code should *do*, which is
  a design question and not a fix.
- **F244(e), the contrast warning as an icon**, stays
  F239. The placement is now specified and the
  contrast metric still does not exist.
- **No change to what the logo does**, again — only where its controls sit.

---

#### Reopened 2026-08-15 — the fifth report, and the flash the first reopening aimed at and missed

Owner-scheduled from F246, the tab's **fifth**
report, filed the day after the first reopening landed. Three of its four limbs
are here; **(d)** is a Phase 4 row and is not.

**Why a reopening again.** Limb (a) makes this milestone's claim false for the
second time — and it is the claim the *first* reopening added, that the position
is never somewhere else between the load and the settle. (b) and (c) are change
requests rather than falsified claims and would ordinarily be their own
milestone. They are here on the owner's instruction of 2026-08-15, taken against
a stated cost: one commit now carries a falsified claim and two requests, which
is what workflow.md's scope gate asks a commit not to do. The
number does not change and work added here marks LNK-M-0064.

##### The position is applied before the first paint

- **F246(a), and it is the option this milestone named at its first reopening
  and did not take.** Removing `#qr` was the other, and it was right as far as it
  went: the fragment is gone and no second mechanism is aiming at the scroll.
  What is left is that **nothing has been restored yet when the browser first
  paints**. `templates/layout.html:31` loads the script `defer`, so
  `static/js/qr-size.js:235-239` takes its `else` branch — readiness is already
  `interactive` when a deferred script runs — and `restore()` therefore fires
  after the parse and after the render-blocking stylesheet, which is after the
  document has been painted at the top. The correction the owner sees is now
  *top → position* instead of *`#qr` → position*; it is the same correction.
- **The variable is round-trip time, which is why four probes and one kept case
  have all said this page is fine.** Measured 2026-08-15 at `928504f`: chromium
  1280×800 against the test instance, `scrollY` traced from `addInitScript` —
  before the document's own scripts — through every `scroll`,
  `readystatechange`, `DOMContentLoaded`, `load` and animation frame, over 3–8
  rounds per profile of Save, the default flag and a code selection. Frames
  painted at the wrong position per Save, the default flag matching within one
  frame:

  | profile | frames | visible for |
  | --- | --- | --- |
  | localhost, unthrottled | 0 | — |
  | 20 ms · 20 Mbps · 1× CPU | 2 | ~85 ms |
  | 40 ms · 8 Mbps · 2× CPU | 4 | ~151 ms |
  | 80 ms · 4 Mbps · 2× CPU | 8–9 | ~300 ms |
  | 150 ms · 1.5 Mbps · 4× CPU | 19–22 | ~650 ms |

  Every round of every throttled profile flashed; no unthrottled round did. The
  owner reaches the demo over the public internet, and the demo serves this
  script byte-identically from an image built 29 seconds after `928504f`.
- **The milestone does not specify the mechanism, and one obvious one is
  already closed.** `script-src 'self'` carries no `unsafe-inline` and no nonce,
  so the restore cannot become an inline script in `<head>`. A parser-blocking
  `<script src>` ahead of the body, a body held back until the position is
  applied, and moving the offset off `sessionStorage` onto something the server
  can render are all available and cost different things — a render-blocking
  request on every page of the dashboard, a paint the reader waits for, and a
  scroll position that leaves the browser. The build chooses, argues it in a
  decision entry, and says what it rejected. Whatever it picks, **the reader is
  never shown a position they did not stand at** — no painted frame of the
  loaded document carries the wrong offset.
  *(Amended at acceptance, 2026-08-16, fact-level. It read **the position is
  applied before the first paint of the loaded document**, not corrected after
  it — which names the shape of one of the three mechanisms this same bullet
  offers rather than the requirement they share, and the build took a different
  one of the three. D196 establishes that the literal reading describes nothing
  buildable: a script running before the first paint runs before the document
  has a height, and `scrollTo` against it clamps to 0. The assertion is
  unchanged, which is why this is an amendment and not a prompt — a held paint
  and a beaten paint both satisfy it, and the menu above already priced the
  hold.)*
  *Enforced by:* the browser case that already asserts this claim, run under
  **emulated network conditions** instead of against localhost.
  `specs/qr-tab-controls.spec.mjs:299` asserts *a save is never shown a position
  the reader did not stand at* and is green on a build that flashes for two
  thirds of a second, because localhost is the one profile where a deferred
  script wins the race by construction. 80 ms with 4 Mbps and 2× CPU would have
  caught it eight frames wide. A case that cannot fail is this tab's recurring
  failure, and this milestone has now produced it twice.
- **It costs no sign-in.** F242 counts two spare
  against `LOGIN_RATE_PER_MIN`, so the emulation goes on the file's shared serial
  page rather than in a new context, and it is scoped and disposed the way that
  file's trail probe already is.

##### The logo controls sit between the colours and the size

- **F246(b)**, owner-set: *"The logo upload thould be below the color pickers
  and above size."* **As this reopening finds it**, the logo controls are
  `partials/link_qr.html:719-748`, ahead of the style form at `:743`; the colour
  inputs are `:784` and `:789` and the size control `:848-862`. The position
  asked for is inside that form's grid, between them.
- **This reverses the constraint recorded one reopening ago, and why the old
  argument no longer applies is the whole of it.** *"It moves within the
  section; it does not move into the style form"* was written from two facts
  that are both still true — a file needs `enctype="multipart/form-data"`, and
  HTML forbids nesting one `<form>` in another. Neither is the whole rule. A
  form-associated control carries a `form="…"` attribute naming the form it
  submits to and may sit **anywhere in the document**: the nesting ban is on the
  `<form>` element, not on its controls.
- **Two things the build shows rather than assumes**, because that attribute is
  where this gets interesting. `hx-trigger="change"` sits on the form element at
  `:722`, and a control outside that element does not bubble `change` to it, so
  the trigger moves onto the input. And htmx serializing a form-associated
  control is a claim about htmx, demonstrated against the running instance
  rather than argued. If neither that route nor another one works, **that is a
  prompt** — not a quiet retreat to the position the tab already has.
  *Enforced by:* a template test asserting document order — the logo controls
  after the background colour input and before the size control — **and** a
  browser case uploading a logo from the moved control and finding it applied,
  because an ordering test cannot tell a working upload from a detached one.

##### The size slider draws its stops

- **F246(c)**, owner-set: *"The Size slider should have visible detents at the
  stop points."* The stops are already on the page: `httpx/web_links.go:1236`
  lists eight, `qrSizeStops` filters them to the code's own floor, and
  `partials/link_qr.html:850-856` puts them in a `<datalist>` the slider names
  with `list=`. Chromium draws tick marks from that datalist, and
  `static/css/input.css:97-103`'s `appearance: none` removes them along with the
  native track. Verified 2026-08-15 by forcing `appearance: auto` on the shipped
  control with nothing else changed: eight marks appear.
- **Restoring the native appearance is not the fix.** `appearance: none` is what
  lets the track and the thumb take theme tokens, which the size control was
  given deliberately; handing it back trades the theme for the marks. The marks
  are drawn by this stylesheet, or by markup this template renders.
- **The difficulty is that the stops are neither evenly spaced nor the same for
  every code.** 128, 256, 300, 512, 600, 1024, 1200, 2048, against a floor that
  moves with the module count — so a fixed background pattern is wrong on most
  codes. A mark's position is `(stop − min) / (max − min)`, and CSS cannot
  compute it without being handed the numbers: a custom property the template
  writes, or rendered elements, are both available and the build argues its
  choice.
  *Enforced by:* a template test that the marks the page draws are exactly the
  list `QRSizeStops` carries — count and values — so a code whose floor drops
  the low stops does not draw them anyway, and a browser case asserting a mark
  is actually visible.

##### Deliberately not done in this second reopening

- **F246(d), the reader's position when a code is selected**, is a Phase 4 row
  and stays with F244(b), which is the same
  navigation. `link_qr.html:236` links each row to `{{.Panel}}`, which
  `httpx/web_links.go:1298-1300` builds as `/links/<id>/qr[?code=<slug>]`, so
  selecting a code loads a different path; `qr-size.js:111-123` stores a
  position on `submit` and `htmx:beforeRequest` and on nothing else, and `:168`
  refuses an offset whose path is not the loaded one. Nothing regressed and
  nothing was ever built for it. It needs the decision F244(b) needs first: what
  selecting a code should *do*.
- **No change to the arbitration between the slider and the number**
  (F240), which this milestone has declined once
  already and which drawing marks does not touch.
- **No change to what the logo does** — a third time, only where its controls
  sit.

---

#### Reopened 2026-08-17 — selecting a code stops being a journey

Owner-scheduled from F246(d) and
F244(b), which are one navigation and are
answered together. Owner, 2026-08-17: *"the selected code is changed with the
default, which means we should be able to prevent scrolling when selecting a
different code as well?"*

**Why a reopening, honestly.** Not workflow.md's trigger: no
claim this milestone makes is false, and its second reopening said in as many
words that (d) was **deliberately not done** and needed a decision first. That
decision is the owner's and has now been taken, and the owner placed the work
here rather than in a new milestone — offered a Phase 3 milestone with a plan
review, a Phase 4 row, and this, they chose this. It is scheduling, and
scheduling is theirs. What the reopening buys is the reason reopening exists at
all: the trail for this surface stays under one number instead of starting a
twenty-fourth row on a phase already eight over its size target. Work added here
marks LNK-M-0064.

##### Selecting a code does not leave the page

- **The finding under the report is that the link page has never been able to
  show anything else.** `internal/httpx/web_links.go:970` calls `h.linkQR` with
  an empty slug, unconditionally, and nothing on that path reads `code` from the
  request — so on the link page *selected* means *default*, and moving the flag
  is the only way to change what the tab draws. That is why making a code the
  default appears to select it, and it is why that path keeps the reader's
  position: it is a write, and writes go through
  D196's restore.
- **A row in the list changes which code the tab shows, without a document
  load.** No load means nothing to remember, nothing to restore and no paint to
  withhold — the position is not preserved so much as never disturbed. The
  thumbnail stays on screen with it, which is F244(b) answered by the same
  change rather than separately.
- **The mechanism is not specified, but one is already in the tree and the
  build says why it did or did not take it.** `partials/link_tabs.html:46-47`
  swaps the whole tab strip with `hx-get` → `#link-tabs`, `hx-select`,
  `hx-swap="outerHTML"`, `hx-push-url="true"`; a code row wants the same request
  with `&code=<slug>`. Whatever is chosen, **the `<a href>` stays a real link to
  something that shows that code**, because a reader with the script blocked has
  only the href — that is D178's standing argument on this tab and it is not
  being spent here.
- **Two things move with it, or the change is wrong in a way the reader meets
  later.**
  - `web_links.go:970` must take the code from the request instead of passing
    `""`, and must keep `linkQR`'s existing fallback for a slug the link does
    not have (`:1156-1163`), which is what stops a stale or invented `?code=`
    from rendering an error where a picture goes.
  - `internal/httpx/web_qr.go:110-117` **deliberately drops `code` on the
    link-page branch** — *"the code being worked on survives the redirect"* is
    written only into the panel branch, and correctly so until now, because the
    link page could not show a non-default code. It must carry it, or the first
    save on a selected code returns the reader to the default one and the
    milestone has moved the bug rather than fixed it.
  *Enforced by:* a browser case that selects a non-default code from the list on
  the **link page**, and asserts three things that fail separately — the scroll
  offset is unchanged, the code the tab draws did change, and the thumbnail is
  still in the viewport. Plus the script-blocked path: the row's `href`,
  followed as a link, still reaches a page showing that code. The offset
  assertion is what F246(d) is; the other three are what stop it passing on a
  page that did nothing at all.

##### Deliberately not done in this third reopening

- **The notice shifts the page on the first write after a load**, and the owner
  deferred it on 2026-08-17: *"I will address the notifications later as they
  are only visible if you notice them at the top of the page."* It is
  F247. The mechanism is
  `pages/link_detail.html:32` rendering the flash **above** the tabs, so the
  first notice grows the page above the QR card and a faithfully restored offset
  shows everything below it shifted by that height; the second write does not
  shift, because the notice was already there. It is a real defect against
  D196's claim, it is not this reopening's, and the row says both.
- **The panel route keeps its own page and its own missing thumbnail.** F244(b)
  closes for the path a reader takes — the list stops sending them there — and
  a reader who opens `/links/<id>/qr?code=…` directly still gets a page with no
  link heading row. That is a surface nothing on the tab links to any more, and
  whether it should exist at all is not this milestone's question.

---

### Reopened — 2026-08-17 (fourth), for the two rows the third one filed

Owner-scheduled at LNK-M-0074's triage
(D203), against a recommendation of Phase 4 for both. The
cost was stated and taken: a fourth reopening on a phase already eight
milestones over its size target, for two rows neither of which falsifies a
shipped claim. What it buys is that the tab's rows are clear before the tag.

Both were filed by the third reopening's own reviewer, which is the pattern
worth naming: this surface's last four defects were all found *after* the
build, by somebody reading the diff rather than the bullets.

#### The thumbnail was already what it should be

**F249 closed without a change, 2026-08-17**,
and the closing is the record. The row said the QR picture beside the link's
name drops a selection when it is clicked, because its `href` is a bare
`?tab=qr` naming no code. Put to the owner as a defect with four ways to fix it,
the answer was that it is not one:

> *"This QR code should always render as the default for the link and clicking
> it should load the QR tab with the default, which I thought was the existing
> functionality."*

It **is** the existing functionality, to the byte.
`internal/ui/templates/partials/link_qr.html:88` links to `?tab=qr` with nothing
else on it, and `internal/httpx/web_links.go:1008` asks for a `code` parameter
that is not there and draws the default — which is also the only picture that
anchor has ever shown (`:1262-1269`, unchanged and deliberate).

**D206 supersedes D204.** D204 answered
*what does the thumbnail mean* with *this link has a QR code*, and drew from it
that the `href` should carry the selected code. The first half stands; the
second was a step the question did not ask for, and it is withdrawn. What
unpicked it was the mechanism: the anchor renders **outside** `#link-tabs` and a
selection is a swap **of** `#link-tabs`, so a server-rendered `href` could only
have kept up by reaching outside the tab — the very reach D204 had just declined
for the reading it rejected. Pricing that reach is what sent the question back,
and the answer was that the destination was never wrong.

**Nothing was built for this limb, and nothing should have been.** The cost of
the round trip is one prompt and no code; the cost of not making it would have
been an `hx-swap-oob` in the tree, a redraw on every swap, and a spent scripting
bound — all to change a behaviour that was correct.

#### A selection that never lands stops leaving an offset behind

- **F250.** `remember()` fires on
  `htmx:beforeRequest` from anything inside `#qr`, which every codes-list row
  now is; `forget()` fires on `htmx:afterSwap`, which a 5xx, an aborted request
  or a reader who navigates away never produces. The offset is keyed to the
  link page's pathname, so the *next* load of that page takes D196's paint hold
  and settles where the reader stood during a selection they have forgotten.
- **The fix reverses a written argument, and that is the work.**
  D196 chose `htmx:afterSwap` over `htmx:afterRequest` on the
  reasoning that *the swap is the observable fact and does not depend on which
  htmx event runs first*. That reasoning is what has to be superseded in
  writing — a decisions.md entry saying why the observable-fact argument loses
  to a request that produces no swap at all — or it is not superseded.
  Changing the event without changing the record is the failure this surface has
  now had three times.
- **Whatever event is chosen, the three reveals D196 argues stay.** A page that
  stays hidden is far worse than a page that jumps, and the `finally`, the
  `load` handler and the four-second timer are what make that true.
  *Enforced by:* a browser case that starts a selection, fails its request, and
  asserts that a subsequent load of the same page is not held and does not move.

---

Rules every milestone inherits — SLO re-verification, privacy stance, permission
seeding, sabotage discipline, OpenAPI and contract parity — are in
README.md and are **not** repeated per file.

## LinkCtrl

phase-details/m50.8.md
