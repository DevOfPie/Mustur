# LNK-W-0060

<!-- Generated by `mustur export`. Do not hand-edit: `mustur verify` fails on drift, and the store is the record. -->

**More than one QR code per link, told apart in the analytics**

work-unit · 2026-08-12

milestone: [LNK-M-0060](../milestones.md#lnk-m-0060)

cites: [LNK-M-0059](../milestones.md#lnk-m-0059)

cites: [LNK-D-0073](../decisions.md#lnk-d-0073)

cites: [LNK-D-0076](../decisions.md#lnk-d-0076)

cites: [LNK-M-0042](../milestones.md#lnk-m-0042)

cites: [LNK-M-0072](../milestones.md#lnk-m-0072)

cites: [LNK-M-0074](../milestones.md#lnk-m-0074)

cites: [LNK-M-0058](../milestones.md#lnk-m-0058)

cites: [LNK-M-0040](../milestones.md#lnk-m-0040)

cites: [LNK-F-0222](../findings.md#lnk-f-0222)

cites: [LNK-M-0046](../milestones.md#lnk-m-0046)

## Context

**Depends on:** LNK-M-0059, and the edge is **hard**. LNK-M-0059 rewrites what a QR
style *is* on the surface; adding a second row per link before that means
migrating the same jsonb twice.
**Discharges:** Plan.md's *Not in Phase 2* row **More than one QR code per link,
and per-code scan counts**. It reopens the shape
D73 and
D76
closed, and it does so along the axis those decisions left open.

One link, one code. A workspace running a print campaign and a screen campaign
against the same destination gets one code for both and cannot tell which one
people scanned. `02700_qr_and_campaigns.sql:38-48` made this explicit when it
replaced the non-unique index with `qr_codes_link_key` — *"00600 gave `qr_codes`
a non-unique index on `link_id`, which is the shape for a link with several codes
— different campaigns, different print runs. That is not what this milestone
built."* This is that milestone.

#### Done means

##### Several codes, each with an identity

- `qr_codes_link_key` (`02700:48`) is dropped and the non-unique index returns,
  in migration `037xx`. Every existing row is already unique per link, so no
  data changes.
- Each code carries a short, workspace-visible **label** and a stable
  **identifier that appears in its payload**. The label is what a person reads
  in the interface; the identifier is what the redirect sees.
- A per-workspace cap on codes per link, in the idiom of
  `domain.MaxCampaignsPerWorkspace`. A link with a thousand codes is a link
  whose analytics page cannot be drawn.
- Deleting a link cascades its codes, which the existing foreign key already
  does (`00600:57`).

##### The payload, and the closed vocabulary it must not break

`internal/domain/attribution.go` defines `ClickSourceParam = "src"` (`:36`),
`ClickSourceQR = "qr"` (`:39`) and a **closed vocabulary** at `:44`. A code
identity cannot ride inside `src` without opening that vocabulary to
user-controlled values, which is the property it was closed to have.

- The identity therefore travels in **its own parameter**, alongside
  `src=qr` rather than inside it. The vocabulary stays closed and a test asserts
  it still rejects everything outside the enumerated set.
- The parameter's value is validated against the codes that exist for the link
  resolved. An unrecognised value is recorded as *no code* rather than stored —
  otherwise the parameter is an open write surface reachable by anybody who can
  read a URL.
- `link.QRContent` (`internal/link/qr.go:718-730`) is what builds it, so the
  encoded payload and the redirect's expectation cannot drift apart.

##### Counts, without the column D73 removed

`02700:34` dropped `qr_codes.scan_count` because a write per scan does not fit a
20ms redirect budget and because a scan is already an ordinary click carrying
`?src=qr`. **That reasoning is not reversed.**

- Per-code counts are read from `click_events` through the existing analytics
  path, the same way every other dimension is. No counter column returns.
- The read is bounded the way the other dimension reads are, and the milestone
  states which existing query shape it follows rather than inventing one.
- **Campaign analytics stays deferred.** A per-campaign rollup was kept out of
  this phase because it is a new pass over `click_events` grouped by a mostly-null
  column, stacked on the job LNK-M-0042 was rewritten to fit. Per-code counts are a
  filter on an existing dimension, not a new rollup, and the milestone must stay
  on the right side of that line — if it turns out to need a rollup, that is a
  finding and a prompt, not a quiet addition.

##### The redirect path, and what it owes

Parsing a second query parameter happens on the hot path, so the inherited rule
applies without exception: **the docs/slo.md k6 measurement is
re-run on the built image and cached p99 stays under 20ms.** This is one of the
phase's **three** k6 runs; the others are LNK-M-0072's rolling-deploy
measurement and LNK-M-0074's re-measurement on the final build.

- The parse is a lookup against data already resolved for the redirect. If it
  turns out to need a query, it does not ship in this form.
- The tripwire tests that keep the redirect tree free of session lookups and
  template rendering pass unmodified.

##### Surface

- The link's QR area lists its codes, each with its label, its size and its
  download. The list lives inside whatever LNK-M-0058 decided an on-demand
  panel is; this milestone does not invent a second pattern for it.
- The API gains create, list, update and delete over codes, replayed by the
  contract test per the inherited rule. The existing single-code endpoints
  (`internal/httpx/api_qr.go`) either grow an identifier or stay as the
  default-code shorthand, and **which one is decided in this milestone and
  recorded** — silently changing the meaning of a shipped endpoint is the thing
  the contract test exists to catch.

##### Deliberately not done

- **No per-code destination.** A code that points somewhere else is a link, and
  this product already has links.
- **No dynamic re-pointing of a printed code** beyond what changing the link's
  destination already does — which is the entire point of the product and needs
  nothing here.
- **No per-code expiry or gating.** Gates belong to the link
  (LNK-M-0040); giving a code its own would put two answers on one redirect.

##### Demo

`demoCoverage()` gains a row: one seeded link carries two codes with different
labels and click history against both, because a feature whose whole value is
*telling two things apart* shows nothing when only one exists.

#### Risks

**Moderate, and it lands on the hot path — which is the whole of it.** The
schema change is an index drop and a column add. The analytics read is an
existing shape with one more filter. The risk is entirely in the redirect: a
second parameter parsed on every scan, validated against per-link data, inside a
20ms budget that is already measured and already met.

The unknown is the validation. *Check the identity against the codes for this
link* is a lookup, and whether it can be served from what the resolver already
has in hand or needs something new is not knowable from the plan. If it needs
something new, the honest outcomes are to attribute optimistically and validate
off the path, or to not ship it — and choosing between those is a prompt.

Second: unrecognised identifiers arriving from old printed codes after a code is
deleted. They record as *no code*, which is correct and which means a deleted
code's history stops accumulating rather than being reassigned. That will look
like data loss to somebody, so it belongs in the documentation.

---

### Reopened — 2026-08-12

**The default code cannot be removed, and one control ignores which code is
selected** (F222): *"As long as there are
multiple QR codes any of them should be able to be removed, currently the first
one cannot be removed."* Work below carries LNK-M-0060's number. Reviewed by
LNK-M-0074.

**This milestone's claim about what the default code *is* stops being true.**
The default was the row with the empty slug — that identity is what made it
unremovable, and it is what the owner is rejecting. It becomes a settable
property instead.

#### Done means, as reopened

- **Every code carries a `qrc` tag**, the default included. *(Amended
  2026-08-13, owner-answered, assertion-level and therefore put to the owner
  rather than corrected: a link's **only** code keeps the empty slug and the
  untagged payload. Meeting this literally would hand a slug out the first time
  somebody changed two colours, which rewrites the payload of a picture they may
  already have printed and falsifies LNK-M-0046's shipped claim — still
  printed on the tab — that restyling never changes what a code says. A code
  gains a slug when it **stops being alone**. The owner, asked with three
  options: "The slugless route will be funneled through the default tagging that
  has been or will be added. Existing QR codes should still function once that
  is complete." That is what the build does — an untagged scan resolves through
  the flag, and nothing already printed changes meaning.)* The parameter
  is already named `qrc` (`domain.ClickCodeParam`), so nothing is renamed
  and no printed picture changes meaning. Today's default code gains a
  slug generated the same way every other code's is — owner-answered, over
  a reserved `default` slug and one derived from the link's name.
- **The default is a flag, not the absence of a slug.** Any code can be
  set as the default, and the default is what an *untagged* scan resolves
  through — which is exactly the behaviour the empty slug carries today,
  moved onto a property. A scan arriving with no `qrc` attributes to
  whichever code holds the flag.
- **Every code is removable when more than one exists.** Removing the one
  that holds the flag promotes another; the promotion is stated to the
  reader rather than silent, because it changes where untagged scans land.
  The last remaining code cannot be removed, and that refusal says why.
- **Nothing already printed changes what it means.** A picture printed
  before this milestone carries no `qrc` and attributes to the default,
  which starts as the code it always was. A picture carrying `?qrc=<slug>`
  attributes to that slug exactly as before. Both are asserted, and the
  redirect tree's tripwire tests pass unmodified — this changes
  attribution, not routing.
- **Restore defaults acts on the selected code.** It takes no slug today,
  so pressing it while a named code is selected clears the *default*
  code's style and drops the reader onto the default code. It is scoped to
  the selection, and the redirect returns to the code that was being
  edited.
- The API, the analytics breakdown and `demoCoverage()` follow: the
  breakdown tells codes apart by slug and every code now has one.

#### Risks, as reopened

**This is a data-model change under printed artefacts.** The empty slug is
load-bearing in three places — the service, the SQL guard, and the synthesis of
a default row when none exists — and a migration has to give existing default
rows a slug without changing what any already-printed picture resolves to. The
test that matters is the one asserting a pre-migration picture still attributes
where it always did.

**Promotion has no obviously right target.** Oldest, first-in-list, or the code
the reader was looking at are all defensible, and whichever is chosen is a
decision rather than an implementation detail.

## LinkCtrl

phase-details/m50.md
