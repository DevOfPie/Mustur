# LNK-W-0030

<!-- Generated by `mustur export`. Do not hand-edit: `mustur verify` fails on drift, and the store is the record. -->

**Destination blocking: tiers and logging**

work-unit · 2026-08-01

milestone: [LNK-M-0030](../milestones.md#lnk-m-0030)

cites: [LNK-M-0016](../milestones.md#lnk-m-0016)

cites: [LNK-D-0013](../decisions.md#lnk-d-0013)

cites: [LNK-M-0033](../milestones.md#lnk-m-0033)

cites: [LNK-M-0039](../milestones.md#lnk-m-0039)

cites: [LNK-M-0041](../milestones.md#lnk-m-0041)

cites: [LNK-M-0047](../milestones.md#lnk-m-0047)

cites: [LNK-M-0052](../milestones.md#lnk-m-0052)

cites: [LNK-F-0014](../findings.md#lnk-f-0014)

cites: [LNK-M-0036](../milestones.md#lnk-m-0036)

cites: [LNK-F-0026](../findings.md#lnk-f-0026)

cites: [LNK-M-0051](../milestones.md#lnk-m-0051)

cites: [LNK-F-0077](../findings.md#lnk-f-0077)

cites: [LNK-D-0046](../decisions.md#lnk-d-0046)

cites: [LNK-D-0091](../decisions.md#lnk-d-0091)

cites: [LNK-M-0032](../milestones.md#lnk-m-0032)

## Context

**Depends on:** LNK-M-0016. **Discharges:** Link-management row *Malicious
destination blocking, tiered by confidence*; the tiers and logging legs of the
Security blocking row. Plan.md mandates these ship before disputes.

Two threat models wear one name. Phase 1's refusals protect *this instance* from
being an SSRF proxy. This milestone protects *visitors* from a destination hostile
to them. They must not share an override switch.

#### Done means

##### The three tiers

| Tier | Contents | Overruled by |
| --- | --- | --- |
| Unappealable | Phase 1's SSRF refusals, untouched | Nothing |
| High confidence | Curated embedded exact-host list, shipped like `reserved.txt` | Editing the list and rebuilding |
| Low confidence | Heuristics + a runtime-mutable Postgres blocklist | The instance owner, from the review queue |

- A test asserts **no configuration, list entry, or future review path can accept
  a metadata or private address**. The unappealable tier shares no override switch
  with anything.
- Heuristics never write into the embedded tier — asserted structurally, not by
  convention. Confining that tier to exact matches is what keeps a false positive
  there from costing a rebuild often enough that operators route around it.
- Low-confidence heuristics: punycode homographs, credentials in the URL,
  shortener chains. **Freshly-registered domains is excluded** (decision D13): it
  needs a domain-age data source, which means egress, which collides with the
  no-destination-leaves-the-box promise. Noted as exactly what LNK-M-0033's
  opt-in feed path can supply. The scope row is annotated rather than silently
  shipped short.
- `LINKCTRL_DESTINATION_BLOCKLIST` seeds the runtime Postgres list and keeps
  feeding it — the env list keeps working.
- Every refusal's 422 carries a reason code naming its tier and rule.

##### Coverage and logging

- The check runs at every destination-writing surface that exists when this ships:
  link create, link update, and the root redirect — all three `ValidateDestination`
  call sites, kept by test. Re-checking already-accepted links is **not** included
  (Plan.md calls it a separate job and a separate decision).
- **Every later milestone that adds a destination-writing surface declares this
  one as a dependency and passes the full tier check**, asserted by its own tests:
  LNK-M-0039 rule destinations, LNK-M-0041 split-test variants,
  LNK-M-0047 webhook targets. This closes the bypass the plan review found in
  two of three candidate orderings.
- A blocked attempt writes an audit event with `ip_prefix` and the attempted URL
  stored as evidence. Wherever that URL is displayed it is escaped and defanged —
  a test plants a `javascript:` URL and an HTML-bearing URL and proves both
  render inert.

  *(Amended 2026-08-05 on LNK-M-0052's F14. It read
  "**Every** blocked attempt writes an audit event", which is no longer true and
  was the defect: this is the only audited action recording something that did
  **not** happen, so unlike every other one it has no successful state change
  bounding how often a caller can provoke it — a holder of an ordinary editor
  role could loop link creation against `http://127.0.0.1/` and add a row per
  request, on an instance whose audit retention defaults to keep-forever. The
  write is now bounded per actor **and per refusal code**, `BlockedAuditRatePerMin`
  in `internal/link/blocking.go`. The per-code half is what keeps it a bound
  rather than a suppression tool: a refusal code nobody has provoked before is
  always recorded, however hard another is being hammered. The block itself is
  untouched — the bound is on the row, never on the refusal.)*
- Blocking runs only on the management path. The redirect tree is untouched and
  its tripwire tests pass unmodified.

#### Reopened 2026-08-01 — one character defeats the tier that has no override

LNK-M-0030 shipped and its strongest claim turned out false. Reopened by the owner on
LNK-M-0036's triage rather than succeeded, per
workflow.md's rule, so the correction carries this milestone's
number.

**What is false.** The bullet above says *no configuration, list entry, or
future review path can accept a metadata or private address*. A **trailing dot
on the hostname** accepts one. `POST /api/v1/links` with
`http://169.254.169.254./latest/meta-data/` answered **201** on a live instance
while the dotless form answered 422; `127.0.0.1.`, `10.0.0.1.`, `2130706433.`,
`0x7f000001.`, `localhost.` and `foo.localhost.` all behave the same way, and
the high-confidence embedded list is defeated identically —
`metadata.google.internal.` is accepted although that exact host is in
`blocked_hosts.txt`. `PATCH` is affected too, the stored value is 302'd verbatim
at serve time, and **no `destination.blocked` audit row is written**, so nothing
records the attempt. Recorded as F26; the fix is in
spec here.

**What it is, and is not.** It is a stored **open redirect**, not SSRF.
LinkCtrl never makes a server-side request to a destination — the only two
`http.Client`s in the tree are the opt-in feed and the container healthcheck —
so the victim's own browser makes the request and the attacker cannot read the
response. That is why this reopens LNK-M-0030 at *high* rather than critical, and the
distinction belongs in the fix's own reasoning.

##### Done means, for the reopening

- **The dot is canonicalised away, not rejected.** `destination_test.go:133`
  asserts `https://example.com./` is **accepted**, and it is right to: a
  trailing dot is a fully qualified name, not a malformed one. A fix that
  refuses dotted hosts breaks a passing test for the wrong reason.
- **One normalisation point, covering all three tiers.** The DB tier already
  trims (`HostCandidates`, `blocking.go:298`) and `checkListEntry` already
  refuses a dotted *entry* — so today the write side normalises and the read
  side does not. Folding the host once in `ValidateDestination`, before the
  address, numeric and localhost checks, closes the unappealable tier and the
  embedded lookup together; a second hand-written trim in a third place is the
  shape that produced this.
- **A test that feeds a trailing-dot host to each tier.**
  `TestUnappealableTierHasNoOverrideSwitch` walks `DestinationPolicy` by
  reflection looking for a *field*; it never tries a *host*, which is why a
  promise about what can be accepted was guarded by a check on struct shape.
  The new test asserts refusal for the metadata address, a loopback literal, an
  obfuscated numeric form, `localhost.` and an embedded-list host, and asserts
  `https://example.com./` still passes.
- **The audit consequence is covered**: a refused dotted host writes its
  `destination.blocked` row like any other refusal.
- `blocking_test.go:441-444` already asserts this exact class for the Postgres
  tier, calling it *"a one-character bypass"*. The class was known and fixed in
  one of three places; say so in the commit rather than presenting it as new.

#### Reopened 2026-08-04 — the same claim, a different alphabet

LNK-M-0030 was reopened once already, above, and the fix for that one is correct for what
it names. This is a second reopening of the same bullet by a different mechanism,
from LNK-M-0051's triage, so the correction again carries this milestone's
number.

**What is false.** The bullet still says *no configuration, list entry, or future
review path can accept a metadata or private address*, and `docs/SECURITY.md:40`
says *"a name cannot be spelled past a check that would otherwise refuse it"*.
Both are false for a host written outside ASCII. Reproduced on a live instance
against the current build — `POST /api/v1/links` accepted and stored all five of:

```
http://169。254。169。254/latest/meta-data/     (U+3002 IDEOGRAPHIC FULL STOP)
http://１６９.２５４.１６９.２５４/                (fullwidth digits)
http://ｌｏｃａｌｈｏｓｔ/                        (fullwidth Latin)
http://metadata。google。internal/computeMetadata/v1/
http://127。0。0。1/admin
```

while the control `http://169.254.169.254/` answered **422**. No
`destination.blocked` row is written for any of them, so nothing records the
attempt. Recorded as F77.

**Why every tier misses it.** `internal/link/destination.go:147` is the whole of
host canonicalisation and folds only the ASCII dot. `looksNumeric` finds the last
label with `strings.LastIndexByte(host, '.')`, which finds nothing in a host whose
separators are U+3002, so the entire host becomes "the last label" and the digit
scan exits on the first non-digit. The `localhost` test is an equality. And
`isHomograph` returns early unless a label already starts `xn--`, so a **raw**
Unicode host is never examined by the one tier built for lookalikes.

**It reaches the server, not only the visitor's browser.** The review's first
reading was that Go's client performs no IDNA and LNK-M-0047's webhooks were therefore
immune. That was disproved by measurement: `net/http`'s `canonicalAddr` calls the
standard library's own vendored `golang.org/x/net/idna`, and a dial probe showed
the stored form connecting to `169.254.169.254:80`. The private-address line still
holds, because `guardDial` judges the **resolved address** — but the Postgres
blocklist, the embedded high-confidence list and the homograph tier are all
name-based, so this server can be made to POST a signed workspace payload to a
host the operator blocked.

**What this reopening must not do.**

- **Canonicalise, never reject.** Refusing non-ASCII hosts closes the hole and
  kills `müller.de` and `テスト.example`, which a recorded decision explicitly
  protects as ordinary names. F26's lesson applies again and is harder here.
- **Do not hand-roll a separator table.** Mapping U+3002, U+FF0E and U+FF61 closes
  three of the five spellings above and misses the two that carry no separator at
  all. A fix that looks complete and is not is worse than none.
- **The stored value must be the ToASCII form**, which is D46's own rule — what the
  tiers judged is what the visitor is handed. Storing the percent-encoded original
  re-opens this immediately.
- The dependency is **D91, owner-approved on LNK-M-0051's triage**: `golang.org/x/net/idna`
  goes into go.mod. Do not re-litigate it and do not look for a way around it.
- Expect `isHomograph` to start firing on input it never saw before, because
  ToASCII produces the `xn--` labels its prefix test is written against. That is
  the check working as designed for the first time; if it now refuses something it
  should not, that is in spec here.

#### Risks

Heuristic false-positive rate is unknown until real use; reason codes and
LNK-M-0032's queue are the mitigation. The embedded list's initial contents
need owner sign-off.

## LinkCtrl

phase-details/m30.md
