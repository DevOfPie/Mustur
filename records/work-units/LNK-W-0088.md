# LNK-W-0088

<!-- Generated by `mustur export`. Do not hand-edit: `mustur verify` fails on drift, and the store is the record. -->

**Runtime lifecycle: an add-on arrives and leaves without a reboot**

work-unit · 2026-08-18

milestone: [LNK-M-0088](../milestones.md#lnk-m-0088)

cites: [LNK-M-0079](../milestones.md#lnk-m-0079)

cites: [LNK-M-0081](../milestones.md#lnk-m-0081)

cites: [LNK-M-0082](../milestones.md#lnk-m-0082)

cites: [LNK-M-0087](../milestones.md#lnk-m-0087)

cites: [LNK-M-0061](../milestones.md#lnk-m-0061)

cites: [LNK-M-0062](../milestones.md#lnk-m-0062)

cites: [LNK-M-0089](../milestones.md#lnk-m-0089)

cites: [LNK-D-0098](../decisions.md#lnk-d-0098)

cites: [LNK-D-0018](../decisions.md#lnk-d-0018)

cites: [LNK-M-0091](../milestones.md#lnk-m-0091)

cites: [LNK-M-0090](../milestones.md#lnk-m-0090)

cites: [LNK-M-0086](../milestones.md#lnk-m-0086)

cites: [LNK-M-0016](../milestones.md#lnk-m-0016)

cites: [LNK-M-0085](../milestones.md#lnk-m-0085)

cites: [LNK-D-0368](../decisions.md#lnk-d-0368)

cites: [LNK-F-0335](../findings.md#lnk-f-0335)

## Context

**Depends on:** LNK-M-0079 *(the loader and verification it drives)*,
LNK-M-0081 *(grants are resolved at activation)*, LNK-M-0082 *(removal
is what creates an orphan)*, LNK-M-0087 *(hard, added 2026-08-23 — removal
has a pool to drain, and the bullet below is unsatisfiable without one)*.
**Discharges:** the install/remove halves of the owner's Add-on manager answer
(phase-4-candidates.md).
Split from the manager surface at the plan's review, 2026-08-18 — the LNK-M-0061/LNK-M-0062
precedent: an upload surface and the thing it feeds are two reviewable units,
and this is the one whose Risks called it the sharpest permission the UI has
ever offered.

LNK-M-0079's lifecycle is a directory read at boot. This milestone makes arrival and
departure runtime acts: a module is installed, verified, activated, and removed
without restarting the instance — the mechanism LNK-M-0089's page will
drive, built and reviewed on its own before a page makes it two clicks.

#### Done means

- **The permission exists before the surface**: `addons.manage`, an
  instance-principal scope in the D98 pattern — enumerated, **seeded by an
  insert-and-grant migration** (the 00800 pattern; `03400_instance_principal.sql`
  is the instance-scope precedent), and non-delegable to API keys. The
  milestone records its D18 limb per the inherited rule; the working argument
  is the second limb — a key that can add executable capability has widened
  its own reach past any scope it was issued with.
- **Install is an upload, never a fetch**: a `.wasm` + manifest pair arrives
  in the request body, runs LNK-M-0079's checksum verification and manifest parsing
  before anything activates, and **nothing is fetched from a URL**.
  *(**Superseded 2026-08-25 as to the reason, not the behaviour.** This bullet
  went on to say a fetch *"would be the SSRF-shaped convenience this product
  refuses everywhere else, and its absence is deliberate"* — presented as a
  stance when **no decision backed it**: `decisions.md` has no entry and
  phase-4-candidates.md, which records the owner's
  planning answers, never mentions upload, fetch or URL. The owner's actual
  intention was local and direct-URL installs with a module store on top of
  them, stated 2026-08-25. What LNK-M-0088 built is correct and unchanged; what was
  wrong is the sentence claiming somebody chose it.
  LNK-M-0091 adds the URL route on
  LNK-M-0090's fetch, and this milestone's upload path stays exactly as
  it is. See decisions.md.)*
  *(**Landed 2026-08-27.** LNK-M-0091 shipped, so the second half of
  this bullet — *nothing is fetched from a URL* — is now false of the product
  and true of this milestone's own path, which is unchanged. An install may
  name a bundle URL and the `sha256` it must hash to; the address policy
  LNK-M-0090 built is what answers the argument this bullet used to make, and the
  operator's digest is what makes the trust bounded. This milestone's tests,
  its refusals and the directory it writes are untouched, and a test installs
  the same module both ways and compares the two directories byte for byte.)*
  The operator's boot-directory route from LNK-M-0079 keeps working unchanged.
- **Activation is atomic with the stored state**: the file set and the
  manifest record move together, so a crash mid-install leaves either the old
  world or the new one, never a half — asserted by a test that kills the
  sequence between steps.
- **Removal unloads without a reboot**, and the unload semantics are this
  milestone's design work stated in its diff: in-flight invocations complete
  or are interrupted (chosen and recorded), held instances are released, and
  the leak risk is bounded by test — repeated install/remove cycles hold
  resident memory flat within a stated tolerance.
- **Removal drains the add-on's pool** (LNK-M-0087's seam, written into
  this file when that milestone was added on 2026-08-23 so it is owned rather
  than discovered). LNK-M-0087 keeps instances rather than destroying them, so
  unloading has something to release that LNK-M-0086 did not have: pooled instances
  for the removed add-on are drained before the module is closed, and a test
  drives install → pool warm → remove and asserts nothing of that add-on
  survives. If LNK-M-0087's design turns out to make this free, say so here rather
  than dropping the bullet.
- **A removed `required` add-on cannot brick the boot it was required for**:
  removal updates the stored state atomically with the file set, so the next
  boot does not stop for a module the operator deliberately removed. Tested.
- **Every lifecycle act is audited**: `addon.installed`, `addon.removed`,
  each naming the module, version and checksum, through the LNK-M-0016 writer.
- **The API is the mechanism** (inherited rule): install and remove land in
  `api/openapi.yaml` under `addons.manage`, replayed by the contract test —
  LNK-M-0089's page calls what this milestone ships, and has no private
  side door.
- **The single-instance gate re-runs against a lifecycle sequence**: LNK-M-0079's
  add-on case grows install-then-remove-then-serve, so *one container is a
  tested configuration* covers the moving version of the claim, not only the
  boot-time one.
- **Deliberately not done:** any page (LNK-M-0089); purge of an add-on's schema —
  removal leaves the orphan LNK-M-0082 makes enumerable, and offering the purge
  choice is the surface's job at the point of decision; upgrade-in-place
  (remove-then-install is the path this phase ships, and a one-step upgrade
  with migration implications is its own future argument).

#### Risks

- Upload-a-binary over HTTP is the risk the split isolated: the scope gate,
  the checksum check and the audit record are all load-bearing, and
  `docs/SECURITY.md`'s operator-responsibilities section gains its add-on
  paragraph here, not at the close.
- Unload correctness in wazero (instance lifetimes, module close semantics)
  is unproven in this codebase; the leak-bound test exists because "release
  the instance" is easy to write and hard to mean.
- A `required` add-on removed *while the instance depends on it for auth*
  (LNK-M-0085 class) changes sign-in behaviour at runtime; the act is the
  operator's and audited, and the consequence — external sign-in disappears,
  local continues — must be stated at the point of removal by whatever
  drives this API.

#### Reopened 2026-08-26

**The leak tripwire's verdict is a coin toss, so the second limb of its own
bullet is not delivered.** `TestRepeatedInstallAndRemovalDoesNotGrowResidentMemory`
takes **one** warm-up cycle and then allows 16 MiB across ten more — a window
that grows about 28 MiB in warm-up alone, so whether it passes depends on how
much of that lands inside the measurement. It failed at LNK-M-0090's gates, and it
fails at this milestone's own commit and at trees LNK-M-0090 never touched; `make
check` being green at acceptance here and at M68 was luck both times.

**There is no leak**, measured rather than argued: growth per cycle falls
5,594 → 2,941 → 1,534 → 1,006 KiB across 5, 10, 20 and 40 cycles, and a
per-cycle leak holds per-cycle cost constant. The figures and the method are in
D368.
So the bullet's first limb — *repeated install/remove cycles hold resident
memory flat* — is true, and the tree is not what needs repairing.

Work below carries LNK-M-0088's number.

#### Done means, as reopened

- **The measurement starts after the resident set settles, not after one
  cycle.** Warm up until two successive readings agree within a stated band, or
  for a stated number of cycles justified by the figures in D368 — and say which
  and why, because *one cycle* was a number nobody had measured against.
- **The assertion is about a steady-state window, not about a total.** What the
  bullet claims is that cycling does not accumulate, so what the test compares is
  growth across a later window against growth across an earlier one of the same
  length. A total that contains arena growth cannot distinguish the two, which is
  the whole defect.
- **It still cannot pass under the sabotage it exists to catch.** Whatever shape
  it takes, removing the compiled-module close must turn it red — that is the
  property it was built for and the one thing the repair may not spend.
  `heldBytes`'s premise is kept — `HeapAlloc` is not this measure, because wazero
  maps compiled code outside the Go heap — but **the evidence that comment quoted
  does not survive this reopening**: the *measured smaller, and passed under the
  sabotage* readings came from the deduped workload, under which nothing could
  have caught that sabotage. The instrument stands on the mapping fact; the
  anecdote is retired in place and says why.
- **It is stable under the flags `make check` actually uses.** `-race
  -covermode=atomic` is how it runs, that is how it went red, and the repair is
  judged by repeated runs under those flags rather than by one green pass.
- **F335 is closed by this reopening** — its
  subject is real, its stated cause was not, and it is already superseded in
  place rather than left to be read as current.
- **Deliberately not done:** widening the tolerance, which was put to the owner
  and declined — a tolerance set above noise nobody has explained will not catch
  the leak it exists for; and any change to install or removal, which the
  measurement says are correct.

#### Risks, as reopened

- **A test relaxed to stop failing is worse than no test**, and that is the
  failure mode here rather than a subtle one: every shape that makes the red go
  away also makes the sabotage green if it is taken too far. The sabotage check
  is the guard and it is a bullet rather than a note for that reason.
- **The figures in D368 were taken on this machine, once.** A repair tuned to
  them may be tuned to this VM, which is the same mistake LNK-M-0086 made with an
  instantiation cost measured on an idle box. Prefer a shape that needs no
  magic number — comparing two windows needs none — over one that hard-codes a
  settling threshold.

## LinkCtrl

phase-details/m67.md
